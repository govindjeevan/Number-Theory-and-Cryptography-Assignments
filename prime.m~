primes=[];
for p=1:15
flag=1;
for a=1:p
    if gcd(p,a)~=1
        continue;
    end
    lhs=a^(p-1);
    lhs=mod(lhs,p);
    if lhs ~= 1
        a
        a^(p-1)
        
        
        flag=0
    end
end

if flag==1
    primes=[primes,p];
end

end
primes